class SelectMultiple{
    private page : any //this page variable would of type page interface IPage.
    private selected_elements: Array<any> //this Array would of type page interface IPage.

    constructor(page: any){ //this page variable would of type page interface IPage.
        this.page = page;
    }

    
}